version: '3.5'

services:
  go2:
    image: "${RABBITMQ_CONSUMER}"
    environment:
      FOO: "BAR"
    networks:
      - proxy
    deploy:
      update_config:
        order: start-first
      labels:
        - "traefik.port=8080"
        - "traefik.frontend.rule=HostRegexp:go_sample.{domain:.*}"
        - "traefik.backend.loadbalancer.swarm=true"

networks:
  proxy:
    external: true
    name: stratsys_swarm_proxy
